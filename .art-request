# Relic art generation request — change a value + push to main to fire the
# `relic art` workflow. Leave `styles=` EMPTY to bake+ship (flat register) to
# the relic-art branch. List ids in `relics=` for a targeted re-roll, or `all`.
#
# FULL RE-BAKE on the GREENSCREEN pipeline: chroma-green screen + chroma keyer
# (holes key inside AND out, feathered edges, de-spill, anti-shadow). All 56.
relics=all
styles=
candidates=1
promote_and_bake=true
nonce=11
