# Relic art generation request. Leave styles= EMPTY to bake+ship (flat) to the
# relic-art branch. List ids in relics= for a targeted re-roll (rest seeded from
# the branch), or `all`.
#
# DRIFT RE-ROLL: greenscreen keying is good; fix 6 that drifted on the full
# regen (skulls + a teal backdrop) — harder negatives + fresh seeds.
relics=jeweler-band morningstar-chip vess-last-wick ring-of-vigor cael-plague-beak chime-of-still-air
styles=
candidates=1
promote_and_bake=true
nonce=12
